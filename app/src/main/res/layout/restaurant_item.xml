<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <ImageView android:id="@+id/Restaurant.item.image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/test_logo" />
    <TextView android:id="@+id/Restaurant.item.name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="dasjdkasdkljas"
        android:textSize="@dimen/Restaurant.item.title"
        android:layout_toRightOf="@id/Restaurant.item.image"
        android:textColor="#ffffff" />

</RelativeLayout>