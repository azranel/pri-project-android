<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="com.restauranto.restaurantoapp.activities.LoginActivity">

    <TextView android:id="@+id/LoginActivity.title"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/LoginActivity.title"
        android:textAlignment="center"
        android:textSize="@dimen/LoginActivity.screenTitle"
        android:paddingBottom="20dp" />

    <TextView android:id="@+id/LoginActivity.loginLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/LoginActivity.loginLabel"
        />

    <EditText android:id="@+id/LoginActivity.loginInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:inputType="textEmailAddress"/>

    <TextView android:id="@+id/LoginActivity.passwordLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/LoginActivity.passwordLabel"/>

    <EditText android:id="@+id/LoginActivity.passwordInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:layout_marginBottom="20dp"
        />

    <Button android:id="@+id/LoginActivity.submitButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/LoginActivity.submitButton"
        android:onClick="signInToRestauranto"/>

</LinearLayout>
